<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.citi.portfoliomanager.dao.mapper.UsersMapper">
    <!--    <resultMap id="simplePerson" type="com.citi.portfoliomanager.entity.User">
            <id column="user_id" property="id"/>
            <result column="user_name" property="username"/>
            <result column="user_real_name" property="realName"/>
        </resultMap>-->

    <!--获取-->

    <insert id="createUser">
         insert into users (id,name,pwd,usertype)
         values (#{id},#{name},#{pwd},#{usertype})
    </insert>

</mapper>
